<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Bordas arredondadas -->
    <corners android:radius="12dp"/>

    <!-- Borda do campo -->
    <stroke
        android:width="2dp"
        android:color="#2f114b"/>

    <!-- Espaçamento interno -->
    <padding
        android:left="10dp"
        android:top="10dp"
        android:right="10dp"
        android:bottom="10dp"/>
</shape>
